#include <stdio.h>
#include <math.h>

int main()
	{
		float a, b, soma, sub, mult, divi;
		
		printf("Digite dois numeros \n", a, b);
		scanf("%f %f", &a, &b);
		soma = a + b;
		sub = a - b;
		mult = a * b;
		divi = a / b;
		printf("soma = %f \n subtracao = %f\n multiplicacao = %f\n divisao =%f\n ", soma, sub, mult, divi);
	}
	
