#include <stdio.h>
#include <math.h>

int main()
	{
		int a, b, resto;
		
		printf("Digite dois numeros \n", a, b);
		scanf("%d %d", &a, &b);
		resto = a % b;
		printf("resto da divisao =%d\n", resto);
	}
	
